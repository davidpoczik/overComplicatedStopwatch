// event listeners 
const startButton = document.getElementById('start'),
stopButton = document.getElementById('stop'),
resetButton = document.getElementById('reset')

export { startButton, stopButton, resetButton }